export default function AeoPage() {
  return (
    <article>
      <h1 className="mb-6 text-3xl font-bold">AEO axis</h1>
      <p className="mb-4 text-[var(--muted)]">
        Beyond design slop, slop-detect measures Answer Engine Optimization: can AI systems like
        ChatGPT, Claude, and Perplexity actually fetch, read, and cite your page? The AEO axis runs
        a separate weighted checklist — robots access, structured data, readable content, and
        similar signals.
      </p>
      <p className="text-[var(--muted)]">
        AEO polarity is inverted compared to slop: higher is better. A page can look clean visually
        but still be invisible to agents; the AEO report surfaces those gaps so you can fix what
        machines see, not just what humans see.
      </p>
    </article>
  );
}
