# Generated / vendored — never reformat
node_modules
package-lock.json
**/dist
**/build
packages/web/public/**
assets-raw
# Data + fixtures we want byte-stable (reformatting can change detector output)
**/calibration/**/*.json
**/fixtures/**
**/*.min.js

# Hand-curated prose — Prettier mangles tables and intentional line wrapping.
# These follow .editorconfig (no trailing-whitespace trimming) instead.
**/*.md
