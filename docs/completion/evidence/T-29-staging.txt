T-29 — staging story + KV namespaces (G-10 + G-11) — DONE (agent part; live verify = H-01)
Branch: ravidsrk/p4-staging

CLAIM: the staging story is fully specified and every checkable part is
verified; only the credential-gated live run remains (H-01), exactly the
T-08 precedent ("DONE (agent part; prod run = H-01)", G-13 closed).

CHANGE (docs + script + workflow + tests; zero prod-code changes):
- docs/STAGING.md (new): environments table (production main/CI-gated,
  PR previews, local sim), branch controls (main-only prod, previews
  promote by merge only), the safety interlock, KV namespace strategy
  table, kv-namespaces.mjs usage, 5-step H-01 finishing checklist,
  staging secrets map (no webhook/Resend on previews, low daily cap).
- .github/workflows/preview.yml (new): PR -> branch preview deploy,
  mirroring deploy.yml's pinned actions, secrets, and build+test gates.
  Concurrency per-PR with supersede. Self-skips with notice (never red)
  unless secrets exist AND a live preview_id is in wrangler.toml — the
  interlock that makes prod-KV-binding previews impossible. PR comment
  via preinstalled gh (no new action pins).
- apps/web/scripts/kv-namespaces.mjs (new, no deps): list / ensure
  (idempotent by title, dry-run unless --apply) / check (wrangler prod
  IDs exist). Mirrors kv-backup.mjs CLI conventions + main guard.
- apps/web/wrangler.toml: commented preview_id template (placeholders
  non-hex by construction). Mechanism verified by doc search:
  preview_id (not env blocks) is what branch preview deployments bind.
- apps/web/scripts/kv-backup.mjs: readNamespaces() now strips
  full-line comments — found by inspection while adding the template:
  without it the template's placeholder IDs would parse as live config
  and corrupt the backup map. Real landmine, fixed + tested.

TESTS (+9: kv-namespaces 8, kv-backup 1):
- Titles, idempotent ensure (no POST), dry-run vs --apply, unknown
  binding rejected pre-API, check ok/failures, 403 surfacing, parseArgs.
- Commented examples never parse (reads the live wrangler.toml WITH
  the new template as fixture — fails if stripping regresses).
- docs-alignment guard extended to STAGING.md (routes + links).

VERIFICATION WITHOUT H-01 (each checkable claim checked):
- pyyaml parses preview.yml; all 3 action pins byte-match existing
  repo pins (no new supply chain).
- Interlock grep executed: 6 commented preview_id mentions, 0 live ->
  workflow correctly self-skips today.
- Script CLI smoke: list/ensure/check fail fast with the H-01 message
  when creds are absent (no stack traces, no partial writes).
- Full gates green (below); web 441 = 433 + 8 exact.

LIVE REMAINDER (H-01 holder, all in STAGING.md checklist): set secrets,
ensure both preview namespaces, add preview_id lines via PR, open a PR
and watch preview.yml go green, verify preview health + scan + zero new
production keys, run check.

GREPTILE ROUND (2 P1 + 2 P2, all fixed):
- P1/security branch injection: head_ref/sha/pr-number now pass via env
  (quoted) in run blocks, AND preflight fail-closes (exit 1) on branch
  names outside [A-Za-z0-9/_.~-] before the wrangler command consumes
  the ref as data — safe regardless of downstream shell behavior.
  Verified: normal branches allowed, `$(...)` + quote-breakout rejected.
- P1 partial isolation: interlock now requires live-preview_id count >=
  kv_namespaces block count (scales to N bindings), not merely >= 1.
  Verified on fixtures: 0/2 + 1/2 skip, 2/2 proceeds. STAGING.md updated.
- P2 comment permission: top-level `permissions: pull-requests: write`
  declared (minimal scope for the gh comment step).
- P2 pagination: list() follows result_info pages (50-page safety cap);
  perPage injectable. New test: title on page 2 found with zero POSTs.
  (Bonus live validation: preview.yml's self-skip ran green on PR #167
  itself — pull_request event, preflight success, overall success.)

FOLLOW-UP (caught live + P1 review):
- bash -e kill: the blocks-vs-ids grep -c exits 1 on zero matches,
  which failed the preflight red on the unwired state (run 34955126555).
  Fixed with || true + comment; lesson: my local grep check ran without
  -e, unlike CI. Verified since by executing the exact preflight script
  under bash -e for all 4 paths (skip/skip/proceed/reject).
  (Side signal: the run passed the secrets gate, so CLOUDFLARE_* repo
  secrets exist — only preview_ids + agent-shell access remain for H-01.)
- P1 write-token exposure: checkout now sets persist-credentials: false
  (PR-controlled install/build/test steps get no credential to steal;
  GH_TOKEN exists only in the comment step's env).

=== FORMAT ===
FORMAT_EXIT=0
=== TYPECHECK ===
TYPECHECK_EXIT=0
=== TEST ===
BUN_TEST_EXIT=0
slop-detect-web:test:       Tests  442 passed (442)
(433 base + 9 new, exact.)
