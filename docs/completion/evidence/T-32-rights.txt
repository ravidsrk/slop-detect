T-32 Export/erasure + retention — evidence (run 20260914-S1)
===========================================================
Branch: ravidsrk/p5-rights → PR #175 → merged 4784a1f (merge commit, no squash)
Gaps closed: G-17 (S1, no export/delete path), G-35 (S2, retention unwritten).
P5 COMPLETE: all 3 exit criteria met (legal pages T-30, export/delete T-32,
retention + H filed T-32/T-31). No owner remainder (H-02 mail secrets
already filed under T-31).

Deliverables (all on main @ 4784a1f):
- GET /api/me/export: session-authed JSON download (watches, listings,
  index, suppression, ephemeralCounters) + unattributability notes.
- POST /api/me/erase: index-recovering, ownership-checked erasure
  (performUnsubscribe per owned domain, stale sweep, suppression +
  RATE_LIMIT counter deletion with honest counts); body.email echo guard;
  idempotent; clears the session cookie.
- _session.ts isForeignOrigin() CSRF backstop; _data.ts deleteSuppression,
  emailCountersPresent, deleteEmailCounters.
- docs/RETENTION.md (key-by-key table grounded in _data.ts TTLs);
  data_exported/data_erased audit events + ALERTS.md row; privacy.md and
  terms.md document the self-serve paths (pinned in legal.test.js).

Greptile: 1 P1 + 2 P2, all fixed + replied in-thread (missing-index
recovery via listWatchesByEmail + pre-rebuild stale diff; null-body 400
guard; counters disclosed + deleted with literal tripwire).

Gates (local, pre-push): format:check exit 0; typecheck 6/6 (one TS2339
on `body` fixed with explicit any);
test green — web 488 passed (41 files; 9 rights + 1 legal pin + 3 review
regressions), core 77, action 7, mcp 14, cli 13.
Final CI on 2dcb1be: all 6 checks pass (incl. Greptile Review).
